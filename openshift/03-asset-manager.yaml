apiVersion: v1
kind: Service
metadata:
  name: asset-manager
  namespace: infraops
  labels:
    app: asset-manager
    app.kubernetes.io/part-of: asset-manager
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: asset-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: asset-manager
  namespace: infraops
  labels:
    app: asset-manager
    app.kubernetes.io/part-of: asset-manager
spec:
  replicas: 2
  selector:
    matchLabels:
      app: asset-manager
  template:
    metadata:
      labels:
        app: asset-manager
        app.kubernetes.io/part-of: asset-manager
    spec:
      containers:
        - name: webui
          # Built in-cluster via the binary BuildConfig (oc start-build
          # asset-manager --from-dir .), which pushes to the internal registry.
          image: image-registry.openshift-image-registry.svc:5000/infraops/asset-manager:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: database-user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: database-password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: database-name
            # The app reads DATABASE_URL; build it from the secret values.
            - name: DATABASE_URL
              value: "postgresql://$(DB_USER):$(DB_PASSWORD)@postgres:5432/$(DB_NAME)?sslmode=disable"
            # Signing key for session cookies (JWT).
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: session-secret
            # Initial admin password used to seed the first account.
            - name: ADMIN_DEFAULT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: admin-default-password
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
